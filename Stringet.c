//lendo string com get, le tudo que é digitado no teclado até o espaço.
#include<stdio.h>
#include<stdlib.h>

int main() {
     char[51];
     printf("informe o nome:");
     //scanf("%s", nome);
     gets(nome);

     printf("mostre o nome digitado: %s\n", nome);

  return 0;
}
