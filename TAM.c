#include<stdio.h>
#include<stdlib.h>
#define TAM 5

int main() {
  int vetNumeros[TAM], i, maior, indiceMaior;
  
  for(i= 0; i<TAM; i++) {
      printf("Informe o numero: ");
      scanf("%i", &vetNumero[i]);
     }

  indiceMaior = 0;
  for(i = 1; i < TAM; i++) {
      if(vetNumeros[i] > vetNumeros[indice Maior]) {
           indiceMaior = i;
   }
}
printf("\n\nMaior = %i, na posicao %i \n\n", vetNumeros [indiceMaior], indiceMaior);
   return 0;
 }
