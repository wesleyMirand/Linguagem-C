/*vetor de 5 posicoes
qual maior elemento que é o 9 */
#include<stdio.h>
#include<stdlib.h>
#define TAM 5

int main() {
  int vetNumeros[TAM], i, maior;
  
  for(i = 0; < TAM; i++) (
      print("Informe o numero: ");
      scanf("%i", &vetNumeros[i] > maior) {
             maior = vetNumeros[i];
       }
      }
   printf("\n\nMaior = %i\n\n", maior);
   return 0;
}
