#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i = 1;
    //sintax do do while
    do{ //vai ser executada pelo menos uma vez sempre
        BlocoDeExecucao
    } while(TesteCondicional);
    
    printf("\n%i\n", i);
   return 0;
}
