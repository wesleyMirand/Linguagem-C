#include<stdio.h>
#include<stdlib.h>
#define TAM 5

int main() {
  int vetNumeros[TAM], i, maior, indiceMaior;
  
  for(i = 0; < TAM; i++) (
      print("Informe o numero: ");
      scanf("%i", &vetNumeros[i] > maior) {
             maior = vetNumeros[i];
             indiceMaior = i;
       }
      }
   printf("\n\nMaior = %i\n\n", maior, indiceMaior);
   return 0;
}
